fn main() {
    /*
    let mut counter = 0;

    while counter != 5 {
        counter += 1;
        println!("The counter is now: {}", counter);
    }
    */


    // Range
    for _number in 0..3 { //: exclusive Range => 제외
        println!("The number is {}", number);
    }

    for _number in 0..=3 { //: inclusive Range => 제외
        println!("The number is {}", number);
    }
}